{
  "extends": "../tsconfig.base.json",

  "compilerOptions": {
    "rootDir": ".",          // compile main.ts & preload.ts in place
    "outDir": "dist"
  },

  /* Match the two entry points (and any future files you add here) */
  "include": ["*.ts"]
}
